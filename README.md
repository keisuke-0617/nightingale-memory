# nightingale-memory

ナイチンゲールメモリー開発用の簡易Flaskアプリケーションです。

## 使い方

1. 依存パッケージをインストールします。
   ```bash
   pip install flask
   ```
2. アプリを起動します。
   ```bash
   python app.py
   ```
3. ブラウザで `http://localhost:5000/` を開くと「ナイチンゲールメモリーへようこそ」と表示されます。
